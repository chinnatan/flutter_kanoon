import 'package:flutter/material.dart';

const boderRadiusAll10 = BorderRadius.all(Radius.circular(10.0));

const all8 = EdgeInsets.all(8.0);
const all16 = EdgeInsets.all(16.0);
const all24 = EdgeInsets.all(24.0);
const all32 = EdgeInsets.all(32.0);

const top8 = EdgeInsets.only(top: 8.0);

const bottom8 = EdgeInsets.only(bottom: 8.0);
const bottom16 = EdgeInsets.only(bottom: 16.0);

const left8 = EdgeInsets.only(left: 8.0);
